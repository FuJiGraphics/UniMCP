# Project Conventions (template)

Copy this file to `.claude/skills/prefab-inspector/conventions.md` inside your Unity project and edit it to reflect your team's rules. The prefab-inspector skill loads this file at runtime, so changes take effect without modifying code.

## Prefab naming

<!-- Example: prefabs must match their root script class name (PopupLogin.cs ↔ PopupLogin.prefab). -->

## Script / class naming

<!-- Example: UI components use the `Ui` prefix, popups use `Popup`, canvases use `Cvs`, list cells use `Cell`. -->

## Serialized field naming

<!-- Example: Button fields start with `_btn`, TextMeshProUGUI fields with `_txt`, RectTransform with `_rt`, etc. -->

## Ignored paths

<!-- Example:
- Assets/Plugins/**
- Assets/Z_External/**
-->
